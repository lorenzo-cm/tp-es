// ../components/profilePage/MainContent.tsx
import React from "react";

interface MainContentProps {
  section: string;
}

export const MainContent: React.FC<MainContentProps> = ({ section }) => {
  return <div>{/* Render content based on the section */}</div>;
};